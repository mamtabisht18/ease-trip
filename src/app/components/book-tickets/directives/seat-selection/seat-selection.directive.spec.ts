import { SeatSelectionDirective } from './seat-selection.directive';

describe('SeatSelectionDirective', () => {
  it('should create an instance', () => {
    const directive = new SeatSelectionDirective();
    expect(directive).toBeTruthy();
  });
});
